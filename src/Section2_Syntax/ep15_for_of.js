let testResults = [90, 100, 67, 75, 80];

for(let testResult of testResults) console.log(testResult);
        // Similar to for-each Java loop, a for loop with simpler syntax
        // for(let variable of array)



/** Math.sign method **/
console.log(Math.sign(-10) + '\n'
            + Math.sign(10) + '\n'
            + Math.sign(0) + '\n'
            + Math.sign(NaN) + '\n'
            + Math.sign("str"));

/** Math.trunc method **/
console.log(Math.trunc(-3.845));